.DS_Store
*.tmp
*.lock

# Anchored to this directory. As bare patterns these matched at any depth, and
# on a case-insensitive filesystem `TEMPER.md` also matched
# .claude/commands/temper.md (now critique-diff.md) — which is why the slash
# command was never committed to the temper repo.
/temper.config
/TEMPER.md
