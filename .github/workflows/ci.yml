name: CI

on:
  push:
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uvx ruff check py/src/
      - run: uvx ruff format --check py/src/

  test:
    name: Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install -e ".[dev]"
      - run: pytest py/tests/ -v

  smoke:
    name: Smoke Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install -e .
      - name: Help checks
        run: |
          python -m bonsai_python --help
          python -m bonsai_python.pyfindrefs --help
          python -m bonsai_python.pyfindunused --help
          python -m bonsai_python.pymove --help
          python -m bonsai_python.pymovesymbol --help
          python -m bonsai_python.pyrename --help
          python -m bonsai_python.pysignature --help
      - name: End-to-end
        run: python -m bonsai_python.pyfindrefs "bonsai_python.pyfindrefs:find_refs"
